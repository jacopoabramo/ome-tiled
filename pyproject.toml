[project]
name = "ome-tiled"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jacopo Abramo", email = "jacopo.abramo@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ome-tiled = "ome_tiled:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
